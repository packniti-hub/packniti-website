PACKNITI V5 — IMAGE FIX

Replace:
1. cart.js (repository root)
2. checkout-image-fix.js (repository root)

This version uses assets/product-gallery/{size}.png as the primary local product image, with catalogue PNG/JPG fallbacks.
It also keeps cart +/− at 100 boxes per click.

No checkout.js change is required.
