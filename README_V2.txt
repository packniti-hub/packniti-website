PACKNITI PRODUCT PAGE V2 — VISUAL + SEO FIX

Replace these files in your existing PackNiti repository:

1. product.css
2. product.js
3. product.html
4. Every file inside boxes/ (all 39 product landing pages)

Do NOT delete or replace your existing assets, data, boxes.js, checkout files, or catalogue files.

This version fixes:
- Product-page header now uses the real PackNiti logo and matches the main website navigation.
- Product image is contained at the correct aspect ratio; no 120% crop/overflow.
- Main product image is presented in a premium rounded card with a gallery thumbnail strip.
- Product information uses the same typography, spacing, colors, pills and rounded UI language as the main PackNiti website.
- Cart is visible in the product-page header.
- Contact Us CTA is consistent with the homepage.
- Footer matches the existing PackNiti website.
- Related products are displayed as polished cards.
- Product pages are explicitly indexable (the previous generic product.html shell had noindex in its static head).
- Dynamic product schema remains active.

IMPORTANT:
The header references your existing repository asset:
/assets/packniti-header-logo.png
Do not delete that existing asset.

After replacement, test locally before committing to GitHub.
